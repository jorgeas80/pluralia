version: "3.9"
services:
  pluralia-api:
    build: .
    container_name: pluralia-api
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app/backend
    environment:
      - PYTHONUNBUFFERED=1
